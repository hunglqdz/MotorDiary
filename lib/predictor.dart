import 'package:flutter/material.dart';

class PredictorSetting extends StatelessWidget {
  const PredictorSetting({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
