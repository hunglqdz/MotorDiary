import 'package:flutter/material.dart';

class MissingEvents extends StatelessWidget {
  const MissingEvents({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Missing'),
    );
  }
}
